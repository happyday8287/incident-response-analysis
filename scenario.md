## Incident Scenario

Multiple failed login attempts were detected from a single IP address (10.0.0.50).

This activity may indicate a brute-force attack attempting to gain unauthorized access to the system.

The objective is to analyze the activity and determine appropriate response actions.
